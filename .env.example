SESSION_SECRET=replace-with-a-long-random-secret
KV_REST_API_URL=https://your-upstash-redis-url
KV_REST_API_TOKEN=your-upstash-rest-token
# Vercel/Upstash may also provide these names automatically:
UPSTASH_REDIS_REST_URL=https://your-upstash-redis-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-rest-token
BANK_DATABASE_KEY=bank-portal-database
ADMIN_PASSWORD=replace-with-admin-password
# Optional future email delivery settings. Without these, approval emails are
# generated into the local emailOutbox in the JSON database for review.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=operations@turkishbank.example
